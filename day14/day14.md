# 亮哥你好:

##### 今天我主要学到了下面这几点
1. 利用画布实现了一些ps的基本功能 "导入图片""画笔功能""拖拽功能""颜色通道"的功能
2. 可以用伪类实现一个小箭头 将高度宽度设为零. 然后设置border的宽度高度 当设置左边距的颜色后会显示为一个指向右的箭头
3. chrome浏览器可以直接在浏览器编辑js文件并保存到项目目录中
4. input type=file 时, 因为浏览器为了用户信息安全, 将文件目录进行了加密, 我们不能很据目录加载图片. 因此我们采用了文件流的方法
    利用FileReader对象来接受文档流
5. 字符串转为数字的简单方法利用 "1"*1 或者 "1"-0 或者 "1"/1